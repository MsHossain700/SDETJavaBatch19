What is a Class:
    Classed are simply files that contain a lot of things and methods are
    one of them. Each method contains code to do something. When we want out
    computer to do that specific task, we create the object of that class,
    and we call that method.

Method:
    Method is a collection of java statements which are executed by the
    computer when we call that method.
